export const WIDGET_MAX_WIDTH = {
  xs: '100%',
  sm: 480,
  md: 600,
  lg: 800,
  xl: 1000,
}
export const PAGE_MIN_HEIGHT = '100vh'
export const PAGE_VERTICAL_PADDING = 6
export const LIST_ROW_HEIGHT = 48
const VISIBLE_ROWS = 10.5
/** Only applies for big screens, on small screens the list will only take
 * as much space as it have not to block the cancel and save buttons
 */
export const LIST_VIEWPORT_HEIGHT = VISIBLE_ROWS * LIST_ROW_HEIGHT
export const FILTER_SELECT_MIN_WIDTH = 150
